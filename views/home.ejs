<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Scatch - Login & Signup</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 flex items-center justify-center min-h-screen">

    <% if (error && error.length > 0) { %>
        <div class="absolute top-5 left-1/2 transform -translate-x-1/2 bg-red-500 text-white px-4 py-2 rounded-md shadow-md">
            <%= error %>
        </div>
    <% } %>

    <div class="flex flex-col md:flex-row items-center gap-10 bg-white shadow-lg rounded-lg p-10 w-full max-w-4xl">

        <!-- Signup Form -->
        <div class="p-6 w-full max-w-md bg-gray-50 rounded-lg shadow-md">
            <h2 class="text-2xl font-semibold text-center text-blue-600">Create Account</h2>
            <p class="text-center text-gray-500 mb-5">Join Scatch today!</p>
            <form action="/users/register" method="POST" class="flex flex-col">
                <input type="text" name="fullname" placeholder="Full Name" class="border py-2 px-3 rounded-md bg-gray-200 text-gray-600 mb-3" required>
                <input type="email" name="email" placeholder="Email" class="border py-2 px-3 rounded-md bg-gray-200 text-gray-600 mb-3" required>
                <input type="password" name="password" placeholder="Password" class="border py-2 px-3 rounded-md bg-gray-200 text-gray-600 mb-4" required>
                <button type="submit" class="bg-blue-600 text-white py-2 rounded-md hover:bg-blue-700 transition">Sign Up</button>
            </form>
        </div>

        <!-- Login Form -->
        <div class="p-6 w-full max-w-md bg-gray-50 rounded-lg shadow-md">
            <h2 class="text-2xl font-semibold text-center text-green-600">Login</h2>
            <p class="text-center text-gray-500 mb-5">Access your account</p>
            <form action="/users/login" method="POST" class="flex flex-col">
                <input type="email" name="email" placeholder="Email" class="border py-2 px-3 rounded-md bg-gray-200 text-gray-600 mb-3" required>
                <input type="password" name="password" placeholder="Password" class="border py-2 px-3 rounded-md bg-gray-200 text-gray-600 mb-4" required>
                <button type="submit" class="bg-green-600 text-white py-2 rounded-md hover:bg-green-700 transition">Login</button>
            </form>
        </div>

    </div>
</body>
</html>
