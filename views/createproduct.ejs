<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Product</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="h-screen flex items-center justify-center bg-gradient-to-r from-blue-500 to-purple-600">
    
    <% if (success.length > 0) { %>
        <div class="absolute top-5 bg-green-500 text-white p-3 rounded-md text-center">
            <%= success %>
        </div>
    <% } %>

    <div class="w-full max-w-lg bg-white bg-opacity-20 backdrop-blur-lg p-8 rounded-2xl shadow-lg">
        <h1 class="text-2xl font-semibold text-white text-center mb-5">Create New Product</h1>

        <form action="/products/create" method="post" enctype="multipart/form-data" class="space-y-4">
            <div>
                <label class="text-white block">Product Image</label>
                <input type="file" name="image" id="image" class="w-full p-2 mt-1 border rounded-md bg-white text-gray-800">
            </div>

            <div>
                <label class="text-white block">Product Name</label>
                <input type="text" name="name" id="pname" class="w-full p-2 mt-1 border rounded-md bg-white text-gray-800" placeholder="Enter product name">
            </div>

            <div class="grid grid-cols-2 gap-4">
                <div>
                    <label class="text-white block">Price</label>
                    <input type="number" name="price" id="price" class="w-full p-2 mt-1 border rounded-md bg-white text-gray-800" placeholder="Product price">
                </div>
                <div>
                    <label class="text-white block">Discount</label>
                    <input type="number" name="discount" id="discount" class="w-full p-2 mt-1 border rounded-md bg-white text-gray-800" placeholder="Discount price">
                </div>
            </div>

            <h2 class="text-xl text-white mt-5">Panel Details</h2>

            <div class="grid grid-cols-3 gap-4">
                <div>
                    <label class="text-white block">Background Color</label>
                    <input type="text" name="bgcolor" id="bgcolor" class="w-full p-2 mt-1 border rounded-md bg-white text-gray-800" placeholder="#hex">
                </div>
                <div>
                    <label class="text-white block">Panel Color</label>
                    <input type="text" name="panelcolor" id="panelcolor" class="w-full p-2 mt-1 border rounded-md bg-white text-gray-800" placeholder="#hex">
                </div>
                <div>
                    <label class="text-white block">Text Color</label>
                    <input type="text" name="textcolor" id="textcolor" class="w-full p-2 mt-1 border rounded-md bg-white text-gray-800" placeholder="#hex">
                </div>
            </div>

            <button type="submit" class="w-full bg-white text-purple-600 font-semibold py-2 rounded-md mt-4 shadow-md hover:bg-purple-100 transition">
                Create Product
            </button>
        </form>
    </div>

</body>
</html>
